%legend
  = "Edit photo"

= simple_form_for @photo do |f|
  = render :partial => 'fields', :locals => { :f => f }

  %div{ :class => "form-actions"}
    = f.button :submit, "Update", :class => "btn btn-large btn-warning"
    = link_to "Cancel", photos_path, :class => "btn btn-large"