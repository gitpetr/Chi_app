%footer{ :class => "footer" }
  %div{ :class => "container" }
    = debug(params) if Rails.env.development?
    %ul{ :class => "footer-links" }
      %li=link_to t(".login_link"), new_user_session_path