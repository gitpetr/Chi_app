-# Input: sermons - array of all sermons.

= content_tag :ul, :class => "nav nav-pills nav-stacked" do
	- sermons.each do |sermon|
		li class="#{ active_sermon_sidebar_link?( sermon_path( sermon ) ) }"
			= link_to "#{sermon.title} (#{sermon.preacher})", sermon_path( sermon )
