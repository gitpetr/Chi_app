legend
	= t(".legend")

	= render 'new_sermon_btn'

- if !@sermons.empty?
	div class="row-fluid"
	  div class="row-fluid"
	    div class="span8"
	    	= render 'audio_player'
	    div class="span4"
			  = render partial: 'sermon_sidebar', locals: { :sermons => @sermons }